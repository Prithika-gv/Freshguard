// Ambient module declarations for packages that ship without their own
// TypeScript types and have no @types package. Pre-existing gap in the
// project (not introduced by the telemetry work) - silences noise so real
// type errors are visible.
declare module 'lucide-react';
declare module 'jspdf';
declare module 'jspdf-autotable';
